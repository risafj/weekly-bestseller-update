{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "Attach to SAM CLI",
          "type": "node",
          "request": "attach",
          "address": "localhost",
          "port": 5678,
          // From the sam init example, it would be "${workspaceRoot}/hello-world"
          "localRoot": "/",
          "remoteRoot": "/var/task",
          "protocol": "inspector",
          "stopOnEntry": false
      }
  ]
}